# Candidate Prompt: PPT Deep Search Forward Test

请使用仓库 `SKILL.md`，基于以下输入完成 PPT Deep Search：

```text
candidate/input/
```

Write artifacts under:

```text
.tmp/forward-tests/stochastic-kv-routing-hitl/<run-id>/
```

Use a clear, new, non-existing run id, for example `candidate-YYYYMMDD-HHMMSS`. Do not overwrite or reuse any prior run directory.
